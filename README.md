# 2_bit_adder
A two-bit adder is a circuit the adds together two, 2-bit numbers. The first number, A, can be
representing using bits a1 and a0. The second number, B, is similarly represented.
The output consists of the sum of A and B, represented as two bits (s1 and s0) and one
carry bit (c).
